import { StyleSheet } from "react-native";

export const constraints = StyleSheet.create({
  label: {
    backgroundColor: "gray",
    margin: 5,
    maxWidth: 110,
    justifyContent: "center"
  },
  labelText: {
    color: "white",
    margin: 5,
    fontWeight: "400",
  },
});
